## Fidelity Practice Problems
- run `npm install` to install the dependencies
- run `npm run build` to start the typescript compiler and generate the js files
- run `npm run test` to see the tests pass
